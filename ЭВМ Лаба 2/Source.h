#include <vector>
#include <iostream>


int MinIndex(const std::vector <int>& vec);
int MinIndex_iterator(const std::vector<int>& container);
bool palindrom(const std::vector <int> mass);
int palindrom_iterator(const std::vector <int> mass_it);
std::vector<int> Array(int N);
void printArray(const std::vector <int>& vec);
std::vector <int> dna(const std::string DNA);